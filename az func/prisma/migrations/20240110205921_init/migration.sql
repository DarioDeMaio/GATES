BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Dati] (
    [id] INT NOT NULL IDENTITY(1,1),
    [temperature] FLOAT(53) NOT NULL,
    [humidity] FLOAT(53) NOT NULL,
    CONSTRAINT [Dati_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
